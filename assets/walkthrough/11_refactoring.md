# 🔄 Safe Step Refactoring

Rename steps across your entire workspace fearlessly. Gherkin PowerTools understands the link between `.feature` files and Python `@step` decorators.

## Try it out:
1. Place your cursor on any Gherkin step in a feature file.
2. Press <kbd>F2</kbd> (or <kbd>Cmd+Shift+R</kbd> on macOS).
3. Type the new step text and press **Enter**.

The extension will instantly rename:
- The Python `@step` decorator text.
- Every matching step across all your `.feature` files in the workspace.

<div align="center">
  <img src="https://raw.githubusercontent.com/carlos-camara/vscode-gherkin-powertools/main/assets/rename-step.gif" alt="Rename Step refactoring" width="600" />
</div>
