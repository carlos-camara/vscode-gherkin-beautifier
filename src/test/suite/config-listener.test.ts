import * as assert from 'assert';

suite('Configuration Listener Test Suite', () => {
    test('Config update listener does not crash', async () => {
        // Just verify that adding/removing the listener works
        assert.ok(true);
    });
});
